/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // API routes are now in app/api/ directory
  // No need for Vercel-specific configuration
};

module.exports = nextConfig;



