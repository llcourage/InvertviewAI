load("@aspect_rules_js//js:defs.bzl", "js_binary")

js_binary(
    name = "electron_app",
    entry_point = "dist/main.js",  # Run this JS file with Node
    data = glob([
        "dist/**/*",
        "renderer/**/*",
        "package.json",
    ]),
    # Dependencies from node_modules are linked through targets generated by rules_js
    # The name here must match what npm.npm_translate_lock generates
)
