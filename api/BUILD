# API routes are deployed directly to Vercel
# Vercel automatically compiles TypeScript serverless functions
# No Bazel build needed - kept for reference only

package(default_visibility = ["//visibility:public"])

# Optional: Type checking target (if needed for CI)
# This doesn't build anything, just validates TypeScript
# Uncomment if you want to add type checking in CI:
#
# load("@rules_nodejs//nodejs:defs.bzl", "ts_project")
#
# ts_project(
#     name = "api-types",
#     srcs = glob(["*.ts"]),
#     tsconfig = "//:tsconfig.json",
# )


