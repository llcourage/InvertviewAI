{
  "name": "interview-ai",
  "version": "1.0.0",
  "description": "Interview AI - Desktop and Web Application",
  "private": true,
  "workspaces": [
    "electron",
    "website"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=website",
    "build": "npm run build --workspace=website && npm run build --workspace=electron",
    "electron": "npm run start --workspace=electron"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}


